name: C++ Ubuntu 20.04

on:
  push:
    branches: 
    - "master"
    - "feature/ci"
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Run build script
      run: ./ci/scripts/run_build_ubuntu20_04.sh
