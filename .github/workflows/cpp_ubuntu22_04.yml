name: C++ Ubuntu 22.04

on:
  push:
    branches:
    - "master"
  pull_request:

jobs:
  build:

    runs-on: ubuntu-22.04

    steps:
    - uses: actions/checkout@v3
    - name: Run build script
      run: ./.github/scripts/run_build_ubuntu22_04.sh
